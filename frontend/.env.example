# ============================================
# ImpactMatch Frontend — Environment Variables
# Copy this file to .env and fill in values
# ============================================

# If running frontend separately from backend (not using proxy)
# REACT_APP_API_URL=http://localhost:5000/api

# When using Create React App proxy (recommended for dev), leave blank
